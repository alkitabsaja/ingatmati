{
  "name": "wp-static-site",
  "version": "1.0.0",
  "description": "backup mirror from matikemana.com",
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch-content.js",
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site content/posts/*.md content/pages/*.md",
    "sync": "npm run fetch && npm run build"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@picocss/pico": "^2.1.1",
    "turndown": "^7.2.0"
  }
}
